# Create database connection
# Ingest user's issue preferences (would or would not read)
# Return a list of recommended issues
# Store relationships between issues? (If comics end up in the same user's "would read" piles, assume they have something in common


# -------------------------------------------------------
from generate_issue_seeds import run
run()
